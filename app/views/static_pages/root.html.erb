<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>

  window.images = {
    logo: "<%= image_path('logo.png') %>",
    roll_over1: "<%= image_path('roll_over1.png') %>",
    roll_over2: "<%= image_path('roll_over2.png') %>",
    rolover: "<%= image_path('rolover.png') %>"
  };

</script>

<div id="root"/>
